
#pragma once

#include "Object.h"

/*
struct UI : Obj {
    UI(Obj* prnt, void(*pump_requests)(Obj& ths), void(*output)(Obj& ths)) : Obj("UI", prnt) {
        props.Put("PumpRequests", (Obj*)new Func(this, pump_requests));
        props.Put("Output", (Obj*)new Func(this, output));
    }
};
*/

